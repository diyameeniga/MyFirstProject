<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyFirstProject.ViewViewModels.Controls.Picker.PickerResultsView"
             Title="{Binding Title}">
    
    <ContentPage.Content>

        <Label Text="Hello">
            
        </Label>
        <!-- <StackLayout Padding="0,20">
            <Label Text="Picker VM!"
                   FontSize="Large"
                   FontAttributes="Bold"
                   TextColor="Blue"
                   HorizontalOptions="CenterAndExpand"/>

            <StackLayout VerticalOptions="CenterAndExpand"
                         HorizontalOptions="CenterAndExpand" >
                <Picker x:Name="pickerVM"
                        Title="Select a Character"
                        TitleColor="darkred"
                        SelectedIndex="{Binding SelectedItem}"
                        VerticalOptions="FillAndExpand"
                        ItemsSource="{Binding CarsList}" >
                </Picker>
                      
                <Button BackgroundColor="Transparent"
                        ImageSource="{Binding SubmitButton}"
                        Command="{Binding OnSubmitClicked}" />
            </StackLayout>

        </StackLayout> -->
            
    </ContentPage.Content>
</ContentPage>